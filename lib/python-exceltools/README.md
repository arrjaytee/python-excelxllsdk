- Get addin compiling_addin extsion building and loading?
- Get it loading into a test session?
- Convert python code from python 2
- Get it loading into an Excel session? (write pytest test)
- Does the console window stuff work _at _all_ any more, or can we automate
  creating some other socket/manhole thing?
- This package is for nbuild tooling, so provides the setuptools extension
  dont' provide helpers for installation here, but do it in another package?
  (which is probably the uv/environment venv one).